If you build software in Nigeria, you've felt this pain. Identity checks scattered across providers. Inconsistent JSON. Three integrations for what should be one call.

The Arapoint Developer API was built to fix that.

One base URL — api.arapoint.com.ng. One prefix — slash api slash v one slash developer. One key in your header. Clean JSON in. Clean JSON out. Verifications that come back the same shape every single time.

Send a curl request to slash api slash v one slash developer slash verify slash nin. Pass the NIN in the body. The response builds line by line — surname, given names, date of birth, gender, photo — wrapped in a predictable envelope with a status code, a message, and a request ID you can trace.

Need BVN? slash verify slash bvn. Need education? slash verify slash education. Same shape, same auth, same speed.

Build against the sandbox first. Your key returns deterministic mock data — no wallet charges, no rate-limit surprises while you wire things up. When you're ready, flip one toggle in the dashboard, and the same code now talks to the real registries with full per-call billing. Sandbox to production with no rewrite.

When you need more than one signal at once, call slash verify slash unified. Send identity and checks together — NIN, BVN, and an array of education results in a single request. Bundle anything over three hundred naira and you get an automatic fifteen percent discount on the whole call. One request, one bill, one envelope.

Security is built in. Generate API keys from the dashboard, tag each one to its environment, and lock it to a list of trusted IP addresses. If a request arrives from anywhere else, it never reaches your wallet.

Every call is logged. Every endpoint is metered. Pull live analytics from slash api slash v one slash developer slash analytics to see request volume, latency, and spend per service — broken down by API key.

Start in the sandbox today at arapoint dot com dot n g slash developer. For developer support, reach us at support at arapoint dot com dot n g.
